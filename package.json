{
  "name": "tare-mobile",
  "version": "1.0.0",
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "test": "node --experimental-strip-types __tests__/outbox.test.mts && node --experimental-strip-types __tests__/scan-match.test.mts && node --experimental-strip-types __tests__/formats.test.mts && node --experimental-strip-types __tests__/ocr.test.mts && node --experimental-strip-types __tests__/when.test.mts && node --experimental-strip-types __tests__/reticle.test.mts",
    "typecheck": "tsc --noEmit",
    "build:ios": "eas build --platform ios --profile production",
    "build:android": "eas build --platform android --profile production",
    "submit:ios": "eas submit --platform ios --latest",
    "submit:android": "eas submit --platform android --latest"
  },
  "//overrides": [
    "@expo/vector-icons drifted to 15.1.1 under its caret and pulled expo-font@57,",
    "a version from a much later SDK. It calls getDirectConverter, which does not",
    "exist in expo-modules-core@3.0.30 — the core expo@54 links. The app died at",
    "module registration with NoSuchMethodError before a line of JS ran, and",
    "`expo install --check` reported clean because it validates direct deps against",
    "SDK 54 ranges and never sees a transitive hoist. Pinned to the version expo@54",
    "itself resolves. Remove this when the whole project moves to a newer SDK."
  ],
  "overrides": {
    "expo-font": "14.0.12"
  },
  "dependencies": {
    "@expo/vector-icons": "^15.0.3",
    "@react-native-async-storage/async-storage": "2.2.0",
    "@react-native-ml-kit/text-recognition": "^2.0.0",
    "@sentry/react-native": "~7.2.0",
    "@supabase/supabase-js": "^2.47.10",
    "babel-preset-expo": "~54.0.10",
    "expo": "~54.0.0",
    "expo-blur": "~15.0.8",
    "expo-camera": "~17.0.7",
    "expo-constants": "~18.0.8",
    "expo-dev-client": "~6.0.21",
    "expo-haptics": "~15.0.7",
    "expo-image-manipulator": "~14.0.8",
    "expo-linear-gradient": "~15.0.8",
    "expo-linking": "~8.0.8",
    "expo-location": "~19.0.7",
    "expo-router": "~6.0.0",
    "expo-secure-store": "~15.0.7",
    "expo-sqlite": "~16.0.8",
    "expo-status-bar": "~3.0.8",
    "expo-updates": "~29.0.19",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native": "0.81.5",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.16.0",
    "react-native-url-polyfill": "^2.0.0",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@expo/ngrok": "^4.1.3",
    "@types/react": "~19.1.0",
    "typescript": "~5.9.2"
  },
  "private": true
}
